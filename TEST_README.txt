To run the tests:
- make sure you have all declared dependencies in package.json
- make sure you have a installed chrome browser
- switch to application root folder and run command: npm test

Notes:
Config files are:
- karma.conf.js : Configuration of the test environment such as included files or test framework.
- /scripts/tests/*.test.js : Test scripts.
